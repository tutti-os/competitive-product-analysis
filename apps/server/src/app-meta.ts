/** Single source of truth for app identity, shared by the health route and the
 * Tutti CLI `status` command. Keep in sync with package.json and tutti.app.json. */
export const APP_ID = "product-competition";
export const APP_NAME = "Competitive Analysis";
export const APP_VERSION = "0.2.0";
